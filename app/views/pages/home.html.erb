<%= render "devise/shared/navbar", artist: current_artist %>
<div class="Banner">
  <%# <div class="Banner__heading">
    <p class="Banner__heading-p text-center font-extralight">Sell your creativity in style on create</p>
  </div> %>
  <%= video_tag "banner.mp4", autoplay: :autoplay, loop: :loop, muted: :muted, class: "home_video" %>
  <div class="Banner-heading">
    <div>
      <h4 class="font-bold">Explore the best of Arts and Images</h4><br>
      <h6> Thousands of creatives are selling and getting paid there worth</h6>
    </div>
    <%= form_tag arts_path, method: :get, class: "d-flex w-8/12 mx-auto" do %>
      <%= text_field_tag :query,
        params[:query],
        class: "form-control",
        placeholder: "Search..."
      %>
      <%= button_tag :submit, class: "Banner-heading-button"  do %>
        <i class="fa-solid fa-magnifying-glass"></i>
      <% end %>
    <% end %>
  </div>
</div>

<div class="container-fluid">
  <div class="Style py-20">
    <div class="Style__Row-heading mb-8"><h4 class="font-bold">Explore</h4></div>
    <div class="grid Style__grid  grid-cols-1 sm:grid-cols-2 gap-5 md:grid-cols-3 lg:grid-cols-4 ">
      <% @arts.each do |art| %>
        <div>
          <%# <%= link_to art_path(art) do  %>
            <%# <div><%= image_tag art.image_url, class: "homepage-image img-fluid gallery__img"</div> %>
          <%# <% end %>
          <%= render "devise/shared/modal-home", art: art %>
          <div class="header-item d-flex align-items-center justify-content-between mt-1">
            <div class= "d-flex" style="gap: 0.8rem">
              <%= cl_image_tag art.artist.photo.key, class: "avatar", alt: "avatar" , style: "height:1.3rem"%>
              <div><p><%= link_to art.artist.name, artist_path(art.artist) %></p></div>
            </div>
            <div class= "header-icon d-flex" style="gap: 0.8rem">
              <div><p class="text-[#3449B8] font-bold">£<%= art.price  %></p></div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
